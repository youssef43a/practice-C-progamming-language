//in this exercice you can use
// write _ read system call function or use argument to read data from user
//any other function is forbiden
//now lets start
/*
input:
first digit + operation type + digit 2
output:
resoult of operation type
*/
//firstly i need function that read two nbr from user so i need to creat function like atoi
//to convert nb to integers because i will read the numbers using read function

#include <unistd.h>
#include <>
void read_two_nb_from_user(char  *first_nb, char *last_nb)
{
        printf("hello");
}

int main()
{
        char *first_nbr;
        char *secand_nbr;
        read_two_nb_from_user(first_nbr, secand_nbr);
}